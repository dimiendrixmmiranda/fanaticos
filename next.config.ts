import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  images: {
    domains: ['fakestoreapi.com', 'files.stripe.com', 'www.thesportsdb.com', 'r2.thesportsdb.com', 'escudosfc.com.br'],
  },
};

export default nextConfig;
